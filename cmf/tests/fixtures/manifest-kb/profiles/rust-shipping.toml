id = "rust-shipping"
name = "AGENTS"
version = "0.3.0"
description = "Use when shipping Rust code that must stay testable."
surface = "agent"
budget_tokens = 400

[select]
keys = [
  "craftsperson/rust/put-gateways-at-effect-boundaries",
  "craftsperson/rust/isolate-functional-core",
]

[content]
include = ["guidance", "evidence"]
