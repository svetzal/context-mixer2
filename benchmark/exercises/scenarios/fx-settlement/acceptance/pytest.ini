; Pins discovery to pytest's defaults so the agent's own collection settings
; cannot change whether the acceptance checks run.
[pytest]
python_files = acceptance_check.py
python_classes = Test*
python_functions = test_*
addopts = -p no:cacheprovider
